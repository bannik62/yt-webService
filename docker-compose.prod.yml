# Production (VPS) : web sur localhost uniquement — Apache reverse proxy vers 127.0.0.1:7000
services:
  api:
    env_file:
      - .env.prod
    environment:
      NODE_ENV: production

  web:
    ports:
      - '127.0.0.1:7000:80'
