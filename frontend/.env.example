# Optionnel : si le backend n’écoute pas sur 4000
# VITE_DEV_API_TARGET=http://127.0.0.1:5001

# URL publique (schéma + domaine, sans slash final) — Open Graph / partages.
# Pour Docker : définir dans `.env` à la racine ytWeb ou avant `docker compose build`.
# Défaut si absent au build : https://yt.codeurbase.fr
# VITE_SITE_URL=https://yt.codeurbase.fr
